tree "C:\wamp64\www\BoilerPlateSvelteKit" /A /F | findstr /V /I "node_modules" > "C:\wamp64\www\BoilerPlateSvelteKit\textfiles.txt"


function Write-Tree {
    param (
        [string]$folder,
        [System.IO.StreamWriter]$writer
    )
    
    $items = Get-ChildItem -Path $folder
    foreach ($item in $items) {
        if ($item.Name -ne "node_modules" -and $item.Name -ne ".svelte-kit" -and $item.Name -ne "build") {
            $writer.WriteLine($item.FullName)
            if (Test-Path $item.FullName -PathType Container) {
                Write-Tree -folder $item.FullName -writer $writer
            }
        }
    }
}

$folder = "C:\wamp64\www\BoilerPlateSvelteKit"
$outputFile = "C:\wamp64\www\BoilerPlateSvelteKit\textfiles.txt"

$writer = [System.IO.StreamWriter]::new($outputFile)
Write-Tree -folder $folder -writer $writer
$writer.Close()


Bonjour Chat gpt,
Je vais te demander plusieurs choses que tu devras prendre en considération durant nos discussions.
Je veux que tu me parles en français.
Je veux que tu prennes le rôle d'un développeur expérimenté de 25 années dans le web plus particulièrement dans le front-end.
Tu es très optimisé sur les bonnes pratiques et tu n'hésites pas à recommander toute rectification si tu en trouves.
Tu es une grosse machine en JavaScript, CSS, SCSS.
Tu connais sur la pointe des doigts svelte et sveltekit.
Tu n'hésites pas à recommander toutes les bonnes pratiques si tu trouves quelque chose qui ne va pas concernant svelte et sveltekit.
Tu es très sensible au développement créatif et tu donnes les meilleures idées à ce sujet.
Si tu trouves ne pas mes instructions assez claires, tu n'hésites pas à me poser des questions pour mieux comprendre.
Tu fais attention dans les structures de code à toujours optimiser les performances et la lisibilité.
Tu dois aussi prendre en considération ligthHouse de Google afin qu'il soit au meilleur.
