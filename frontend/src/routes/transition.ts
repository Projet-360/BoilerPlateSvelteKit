export const animateOut = (layoutContainer: HTMLElement) => {
	// Définissez les animations spécifiques pour la sortie de la page 'home'
};

export const animateIn = (layoutContainer: HTMLElement) => {
	// Définissez les animations spécifiques pour l'entrée de la page 'home'
};
